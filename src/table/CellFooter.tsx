import React from "react";
import {CellFooterProperty} from "./PropsTable";

export class CellFooter extends React.Component<CellFooterProperty, any> {
    constructor({props}: { props: Readonly<CellFooterProperty> }) {
        super(props);
    }

    render() {
        return undefined;
    }
}