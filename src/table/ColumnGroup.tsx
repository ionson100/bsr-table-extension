import React from "react";
import {PropsColumnGroups} from "./PropsTable";

export class ColumnGroup extends React.Component<PropsColumnGroups, any>{
    constructor({props}: { props: Readonly<PropsColumnGroups> }) {
        super(props);
    }

    render() {
        return undefined
    }
}