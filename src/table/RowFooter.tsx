import React from "react";
import {RowFooterProperty} from "./PropsTable";

export class RowFooter extends React.Component<RowFooterProperty, any> {
    constructor({props}: { props: Readonly<RowFooterProperty> }) {
        super(props);
    }

    render() {
        return undefined;
    }
}